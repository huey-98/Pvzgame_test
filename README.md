# 街区警戒线

这是依据 `DEVELOPMENT_PLAN.md` 实现的第一版浏览器游戏原型。

## 项目结构

```text
.
├── index.html              # 页面入口，只负责 Canvas 和脚本装载
├── styles/
│   └── main.css            # 页面布局与 Canvas 外观
├── src/
│   ├── config.js           # 敌人、词条、波次和全局配置
│   ├── utils.js            # 通用数学、绘制和文本工具
│   ├── state.js            # 单局运行时状态与重置
│   ├── combat.js           # 生成、射击、碰撞、伤害、经验和词条
│   ├── update.js           # 游戏循环、实体移动和波次推进
│   ├── render.js           # Canvas 绘制和 HUD/界面
│   ├── input.js            # 鼠标与触控输入
│   └── main.js             # 应用初始化与 requestAnimationFrame 入口
├── DEVELOPMENT_PLAN.md
└── README.md
```

模块通过单一的 `window.Game` 命名空间协作，并按照 `index.html` 中的顺序加载。这样不需要构建工具，直接双击入口文件仍然可以运行；后续如果引入打包工具，也可以按职责迁移为 ES Module。

## 运行

直接双击打开 [`index.html`](./index.html)，或在当前目录启动任意静态文件服务器后访问该页面。

## 开发约定

- 新增敌人、词条、波次或平衡参数：优先修改 `src/config.js`。
- 新增战斗规则：放在 `src/combat.js` 或 `src/update.js`，不要写入渲染模块。
- 新增 Canvas 画面：放在 `src/render.js`，通过 `Game.state` 读取状态。
- 新增鼠标、触控或键盘操作：放在 `src/input.js`。
- 需要新增系统时，创建独立文件并在 `index.html` 中明确加载顺序。
- Canvas 使用逻辑分辨率绘制，并根据设备像素比自动提升实际渲染分辨率；调整画面尺寸时优先修改 `src/config.js` 的 `width` 和 `height`。

## 视觉实现

- `styles/main.css` 负责页面容器、响应式比例和外层光晕。
- `src/render.js` 负责游戏内视觉表现，包括背景纵深、HUD 面板、3D 金属城墙、立体人物、悬浮幻形、僵尸模型、子弹拖尾、Boss 光效以及升级和结算界面。
- Canvas 使用逻辑坐标，并根据设备像素比提升实际渲染分辨率；业务逻辑不需要因为高清屏或 Canvas 尺寸变化而改写。

## 当前已实现

- 9:16 竖屏战斗画面，玩家固定在底部中央并自动索敌、自动射击；点击画面可临时手动瞄准。
- 高 DPI Canvas 渲染，适配不同屏幕分辨率，并保留逻辑坐标下的游戏计算。
- 战斗 HUD：顶部显示关卡名称、暂停按钮、波次和经验条；暂停后可继续战斗或退出回到首页。
- 关卡选择界面：第 1 关已开放，第 2、3 关显示为锁定状态。
- 底部防线采用立体金属城墙绘制，包含上斜面、装甲板、立柱、铆钉、阴影和城墙耐久条。
- 玩家采用立体棕色装备士兵和步枪造型；人物结构从上到下为头部、身体、手臂，面部位于最上层，避免被手臂遮挡。
- 人物右侧加入缩小的悬浮“幻形”技能发射器，作为未来温压弹、干冰弹等技能的视觉挂载点。
- 普通僵尸、快跑僵尸、铁桶僵尸与最后一波 Boss 均使用 Canvas 僵尸模型，并保留特殊外观识别部件。
- 普通子弹为白色细长椭圆；冰冻弹为蓝色，燃烧弹为橘色；没有选择特殊弹种时颜色保持白色。
- 子弹从步枪枪口生成，枪口、人物瞄准方向和子弹方向保持一致，修复了从枪身侧面发射造成的索敌偏差。
- 子弹碰撞、击杀、经验、等级上限 10 级、波次推进、Boss 血条、胜利/失败结算与重新开始。
- 升级时暂停战斗，并从数据化词条池中随机提供 3 个不重复选项。
- 已有词条：增伤、连发、射速、齐射、穿透、大口径、暴击、弱点打击、燃烧弹、冰冻弹。

### 当前步枪词条规则

- **齐射**：同一时刻增加平行弹道；基础子弹为 `|`，获得 1 级齐射后为 `||`，不会再扇形改变角度。
- **连发**：每次攻击沿同一条弹道按短间隔连续发射额外子弹；子弹在运动方向上前后排列，而不是同时横向生成。
- **齐射 + 连发**：每一枚连发子弹仍可按照齐射等级分裂为多条平行弹道。
- **燃烧弹 / 冰冻弹**：分别切换为橘色 / 蓝色子弹，并附加持续伤害 / 减速效果。

## 本次对话变更摘要

1. 将最初的单 HTML 文件拆分为页面入口、样式、配置、状态、战斗、更新、渲染、输入和主循环等模块。
2. 建立 9:16 高分辨率 Canvas 画面，并优化背景、面板、按钮、升级界面和结算界面的视觉表现。
3. 统一普通子弹为白色细长椭圆，并增加冰冻弹蓝色、燃烧弹橘色的属性颜色反馈。
4. 修复枪口、索敌方向和子弹生成点不一致的问题，使子弹从步枪枪口沿瞄准轴线发射。
5. 增加战斗暂停按钮和暂停弹窗，支持继续战斗或退出到首页。
6. 增加关卡选择界面；当前仅第 1 关解锁，第 2、3 关保留锁定入口。
7. 重绘底部防线为立体城墙，并重绘人物为持枪士兵；人物右侧增加缩小、悬浮的幻形技能发射器。
8. 调整人物绘制层级和空间关系，确保头部在上、身体居中、手臂在下，面部不会被手臂遮挡。
9. 将敌人模型调整为普通僵尸、快跑僵尸、铁桶僵尸和 Boss 僵尸的 Canvas 造型。
10. 重做枪械词条逻辑：齐射使用平行弹道，连发使用同一弹道的连续射击，并支持两者叠加。

## 当前限制与后续方向

- 第 2、3 关目前只有锁定展示，尚未开放实际关卡内容。
- 幻形目前是技能发射器的视觉占位，轰炸、温压弹、干冰弹和装甲车等核心技能尚未接入战斗逻辑。
- 分裂僵尸、投掷僵尸以及更复杂的地图互动仍属于后续内容。
- 项目暂不依赖构建工具或外部资源，角色、敌人、城墙和幻形均由 Canvas 程序化绘制。

## 本次修改后的检查方式

在项目根目录执行：

```powershell
Get-ChildItem src -Filter *.js | ForEach-Object { node --check $_.FullName }
git diff --check
```

Windows Git 显示的 `LF will be replaced by CRLF` 是换行符转换提示，不代表代码错误。
